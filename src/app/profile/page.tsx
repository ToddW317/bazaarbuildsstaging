import { profileMetadata } from '../metadata'
import { ProfilePageWrapper } from '@/components/ClientWrapper'

export const metadata = profileMetadata

export default function Page() {
  return <ProfilePageWrapper />
} 